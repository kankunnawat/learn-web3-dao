/** @type {import('next').NextConfig} */
module.exports = {
	reactStrictMode: false,
	images: {
		domains: [
			'lh3.googleusercontent.com',
			'tokens.buildspace.so',
			'cloudflare-ipfs.com',
		],
	},
}
